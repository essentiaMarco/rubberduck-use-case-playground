# Hub control plane (demo-projects/launcher.py)
flask>=3.0

# Pizzeria demo deps are installed into its own package/.venv via setup_and_run.py
# See demo-projects/rubberduck_pizzeria-demoapp/package/requirements.txt
