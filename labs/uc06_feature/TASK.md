# UC-06: Feature planning lab

## Task

Add `--parallel-write` to the HTML builder (see `demoapp/builders/html.py`).

Write tests **first** in `labs/uc06_feature/tests/` before implementing.

## Run

```bash
pytest labs/uc06_feature/tests -v
```

Tests are expected to fail until you implement the feature (or RubberDuck plans it).
